{
  "name": "tehuti-guard-monorepo",
  "private": true,
  "version": "0.2.0",
  "description": "Tehuti Guard — constitutional enforcement for AI actions (MCP proxy + decision API)",
  "repository": {
    "type": "git",
    "url": "https://github.com/Propershare/tehuti-guard.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter tehuti-guard-mcp build",
    "test": "pnpm --filter tehuti-guard-mcp test",
    "lint": "pnpm --filter tehuti-guard-mcp lint"
  },
  "engines": {
    "node": ">=18"
  }
}
